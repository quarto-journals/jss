callouts vs leftbar environment
` ` for code inlines ok?
tables need to be placed at the top of the page
table caption location should be bottom
CodeInput/CodeChunk environment?
Code prompt set to R>
code prompt set on globally
two authors incorrectly wrapping